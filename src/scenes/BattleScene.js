// BattleScene.js

import Phaser from 'phaser';

export default class BattleScene extends Phaser.Scene {
    constructor() {
        super({ key: 'BattleScene' });
    }

    preload() {
        // Carregue recursos específicos desta cena
    }

    create() {
        // Lógica e elementos da cena de batalha
    }

    update() {
        // Lógica de atualização da cena de batalha, se necessário
    }
}
